#include <stdio.h>
int main() 
{
  int length,breadth;
  printf("Enter length of a Rectangle:");
  scanf("%d",&length);
  printf("Enter breadth of a Rectangle:");
  scanf("%d",&breadth);
  printf("Area of a Rectangle:%d",length*breadth);
   return 0;
}
